/**
 * 页脚容器组件
 */
import styled from 'styled-components';

// 通过 styled.footer 生成一个 <footer> 标签. 其中可以包含页脚的内容
const Footer = styled.footer`
  padding: 15px;
  background: #F5F5F5;
`;

// 导出
export default Footer;
